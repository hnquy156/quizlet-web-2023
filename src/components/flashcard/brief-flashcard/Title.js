import { Box, Typography } from '@mui/material';

const Title = () => {
  return (
    <Box display="flex" flexDirection="row" justifyContent="space-between">
      <Typography>Terms in this set (30)</Typography>
      <Typography>Filter</Typography>
    </Box>
  );
};

export default Title;
